<?php

declare( strict_types = 1 );

namespace Tests;

use PHPUnit\Framework\TestCase as PHPUnitTestCase;


/**
 * Tests\TestCase mirrors the application skeleton's base class so
 * `use Tests\TestCase;` resolves. Extends the stubbed PHPUnit
 * TestCase so assert*() methods are accepted.
 */
class TestCase extends PHPUnitTestCase
{
	//
}
